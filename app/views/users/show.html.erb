<h1><%= current_user.username %> Profile</h1>


<%= link_to "CHIRP!!!", new_chirp_path, :class => "btn btn-info" %>

    <% current_user.chirps.each do |chirp| %>
      <div class="panel panel-default message">
        <div class="panel-heading"><%= chirp.message %></div>
        <div class="panel-body"><%= link_to "Delete", chirp_path(chirp), class: "btn btn_delete btn-xs", data: {confirm:"You sure?", method: "delete" } %></div>
      </div>
    <%end%>

<nav class="navbar navbar-fixed-bottom" roll="navigation">
  <span class="pull-right">
    <%= link_to "Edit Profile", edit_user_path(@user) %> |
    <%= link_to "Delete User", user_path(@user), method: "delete" %>
  </span>
</nav>
